
---

### 📝 文件 6: `06_发布标准与注册表_2025版.md`

```markdown
# 06. 发布标准与注册表 (2025版)

ComfyUI 正在从单一的 Git Clone 模式转向 **Comfy Registry (类似 pip/npm)** 模式。

## 1. 准备 `pyproject.toml` (必须)
在插件根目录创建此文件。这是新的官方标准。

```toml
[project]
name = "comfyui-my-awesome-node"
version = "1.0.0"
description = "A comprehensive node pack for image processing"
readme = "README.md"
authors = [
    { name = "Your Name", email = "you@example.com" }
]
license = { file = "LICENSE" }
dependencies = [
    "torch>=2.0.0",
    "numpy",
    "Pillow"
]
dynamic = ["version"]

[project.urls]
Repository = "https://github.com/yourusername/comfyui-my-awesome-node"
"Bug Tracker" = "https://github.com/yourusername/comfyui-my-awesome-node/issues"

# ComfyUI 特有配置
[tool.comfy]
PublisherId = "your_registry_handle" # 在 registry.comfy.org 注册的 ID
DisplayName = "My Awesome Pack"
Icon = "https://example.com/icon.png"